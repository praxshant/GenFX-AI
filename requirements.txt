# ── Core app ──────────────────────────────────────────────────────────────────
# 1.49 is the floor, not a preference: the UI sizes buttons and images with
# width="stretch", which older releases reject outright with a TypeError.
streamlit>=1.49.0
Pillow>=10.0.0
numpy>=1.24,<3
requests>=2.31.0
python-dotenv>=1.0.0

# ── Providers ─────────────────────────────────────────────────────────────────
gradio_client>=1.3.0        # calls image-to-3D Hugging Face Spaces
huggingface_hub>=0.25.0     # optional image + depth backends

# ── Blender runtime ───────────────────────────────────────────────────────────
# The pip build of Blender: it writes real .blend files with no Blender install.
# Wheels are published per Python minor version - 4.5 LTS targets 3.11.
# On other Python versions this line is skipped and GenFX falls back to a
# Blender binary (BLENDER_PATH) or a remote worker (GENFX_BLEND_WORKER).
bpy==4.5.3 ; python_version >= "3.11" and python_version < "3.12"

# ── Tests ─────────────────────────────────────────────────────────────────────
pytest>=7.4.0
